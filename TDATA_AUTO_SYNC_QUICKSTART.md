# 🔄 Быстрый старт: Автоматическая синхронизация TData

## Что изменилось?

**Раньше:**
```
Backend создает tdata → Вручную копируешь в бота → Вручную создаешь архив → Вручную удаляешь файлы
```

**Теперь:**
```
Backend создает tdata → Автоматически копируется в бота → Создаешь архив (один клик) → Автоматически удаляется
```

## Установка (2 минуты)

### Шаг 1: Обновить backend/convert_sessions.py ✅
Уже сделано! Функция `copy_tdata_to_bot()` добавлена.

### Шаг 2: Обновить admin-bot/utils/tdata_service.py ✅
Уже сделано! Методы удаления добавлены.

### Шаг 3: Обновить admin-bot/handlers/tdata.py ✅
Уже сделано! Обработчики обновлены.

### Шаг 4: Обновить backend/server.js ✅
Уже сделано! API endpoint добавлен.

### Шаг 5: Создать папки (если нет)

```bash
# Убедитесь что папки существуют:
mkdir -p admin-bot/tdatas/{ru,us,uk,fr,de,it,es}
mkdir -p admin-bot/archives

# Проверить:
ls -la admin-bot/tdatas/
ls -la admin-bot/archives/
```

## Проверка работоспособности

### 1️⃣ Проверить логи backend

```bash
cat backend/results/session_to_tdata/converter.log
```

Должны видеть строки:
```
✅ TData скопирован в bot: admin-bot/tdatas/ru/9123456789
```

### 2️⃣ Проверить папки в боте

```bash
ls -la admin-bot/tdatas/ru/
# Должны видеть: 9123456789/ (или другие номера телефонов)

ls -la admin-bot/tdatas/ru/9123456789/
# Должны видеть: session.dat, cookies.json, state.bin и т.д.
```

### 3️⃣ Тестировать в боте

```
Пользователь (админ):
1. Нажимает: "📦 Экспорт TData"
2. Видит: "📦 RU (2 папок)"
3. Нажимает: "RU"
4. Видит сообщение: "✅ TData архив создан"
5. Видит сообщение: "🗑️ Папки будут удалены после выгрузки"

Проверяем:
ls -la admin-bot/tdatas/ru/
# Папки должны быть удалены!
```

## Использование (при работе)

### Сценарий 1: Одна новая сессия поступила

```
1️⃣ Backend автоматически:
   - Конвертирует сессию
   - Копирует в бота (admin-bot/tdatas/ru/9123456789/)
   - Отправляет уведомление

2️⃣ Администратор видит:
   "📦 RU (1 папка)"
   
3️⃣ Администратор может скачать:
   - Нажмет "RU" → архив будет создан → папка удалена
   - Или подождать когда накопится больше (например 5 папок)
   - Потом нажмет "RU" → архив со всеми 5 → все удалены
```

### Сценарий 2: Много сессий накопилось

```
1️⃣ Накопилось:
   RU: 5 папок
   US: 3 папки
   DE: 2 папки
   
2️⃣ Администратор выбирает:
   - "RU" → создается tdata_ru_*.zip → удаляются 5 папок
   - "US" → создается tdata_us_*.zip → удаляются 3 папки
   - "DE" → создается tdata_de_*.zip → удаляются 2 папки
   
   Или:
   - "📦 Экспортировать все" → tdata_all_*.zip → удаляются все
```

### Сценарий 3: Проверить статус с backend

```bash
curl http://localhost:3001/api/tdata/status

# Ответ:
{
  "countries": {
    "ru": 5,
    "us": 3,
    "de": 2
  },
  "total_folders": 10
}
```

## Структура папок

Ожидаемая структура:

```
admin-bot/
├── tdatas/
│   ├── ru/
│   │   ├── 9123456789/          ← Один аккаунт
│   │   │   ├── session.dat
│   │   │   ├── cookies.json
│   │   │   └── state.bin
│   │   └── 9987654321/          ← Второй аккаунт
│   │       └── ...
│   ├── us/
│   │   └── ...
│   └── de/
│       └── ...
│
├── archives/
│   ├── tdata_ru_20251116_120000.zip
│   ├── tdata_us_20251116_120015.zip
│   └── tdata_all_20251116_120030.zip
│
└── bot.py
```

## Команды для администратора

Все операции делаются через Telegram бота:

| Команда | Действие |
|---------|----------|
| `/start` | Главное меню |
| `📦 Экспорт TData` | Открыть меню экспорта |
| `RU, US, DE` и т.д. | Создать архив для страны |
| `📦 Экспортировать все` | Создать архив со всеми странами |

**После выбора страны:**
- ✅ Архив создается
- ✅ Ссылка на скачивание показывается
- ✅ Папки АВТОМАТИЧЕСКИ удаляются
- ✅ Меню обновляется (показывает 0 папок для этой страны)

## Мониторинг

### Логи backend

```bash
# Следить за логами в реальном времени:
tail -f backend/results/session_to_tdata/converter.log

# Ищем строки:
# ✅ TData скопирован в bot
# ❌ Ошибка при копировании
```

### Telegram уведомления

Администратор получает автоматические уведомления:

```
✅ Получен новый лог (Russia):
🇷🇺 Успешно конвертирована сессия: 9123456789
📂 TData: /path/to/tdata
🤖 Скопирован в бота для выгрузки

📦 TData архив создан
Страна: RU
Размер: 50.5 МБ
Файл: tdata_ru_20251116_120000.zip
🗑️ Папки будут удалены после выгрузки
```

## Проблемы и решения

### ❓ Статус не обновляется в боте

```bash
# Убедитесь что папки созданы правильно:
ls -la admin-bot/tdatas/ru/

# Если видите: (empty directory)
# То папки еще не скопированы из backend
# Проверьте логи: cat backend/results/session_to_tdata/converter.log
```

### ❓ Архив создается но папки не удаляются

```bash
# Проверьте права доступа:
ls -ld admin-bot/tdatas/ru/
# Должно быть: drwxr-xr-x

# Если не работает:
chmod 755 admin-bot/tdatas/ru/
```

### ❓ Копирование с backend не работает

```bash
# Проверьте что backend папка существует:
ls -la backend/results/session_to_tdata/

# Проверьте логи:
cat backend/results/session_to_tdata/converter.log | grep "TData скопирован"

# Если нет таких строк - значит convert_sessions.py не обновлен
```

## Отключение функции (если нужно)

Если хотите временно отключить удаление файлов:

### Вариант 1: Закомментировать в handlers/tdata.py

```python
# Было:
TdataService.delete_archived_folders(country_code)

# Стало (закомментировать):
# TdataService.delete_archived_folders(country_code)
```

### Вариант 2: Отключить копирование в backend

```python
# Было в convert_sessions.py:
copy_tdata_to_bot(str(tdata_path), country_code)

# Стало:
# copy_tdata_to_bot(str(tdata_path), country_code)
```

## Преимущества новой системы

✅ **Автоматизация** - копирование и удаление идет само собой
✅ **Безопасность** - нет ручного копирования → нет ошибок
✅ **Чистота** - старые файлы автоматически удаляются
✅ **Масштабируемость** - работает с любым количеством стран и аккаунтов
✅ **Мониторинг** - логи и уведомления для каждого этапа

## Следующие шаги

1. ✅ Убедитесь что все файлы обновлены
2. ✅ Создайте папки `admin-bot/tdatas/` и `admin-bot/archives/`
3. ✅ Запустите backend: `npm start` (или `python3 auth.py`)
4. ✅ Запустите бота: `python3 admin-bot/bot.py`
5. ✅ Загрузите тестовую сессию через frontend
6. ✅ Проверьте что папка появилась в `admin-bot/tdatas/ru/`
7. ✅ Нажмите "📦 Экспорт TData" → выберите страну → проверьте что папка удалена

Готово! 🎉
